export function verify(i){ const a = i.abi; const s = (i.repo==="Riverbraid-Wasm-Bridge" && i.ring===2 && i.role==="wasm-bridge-surface" && a.imports.length===1 && a.imports[0]==="verify_packet" && a.exports.length===1 && a.exports[0]==="verify_result" && a.nondeterminism===false); return {pass:s, stationary:s, signal:s?"wasm-bridge:ABI_STATIONARY":"wasm-bridge:DRIFT", reason:s?"Match":"Drift"}; }